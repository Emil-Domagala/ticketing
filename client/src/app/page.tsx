// import buildClient from '@/api/build-client';
// import Quick from '@/components/ui/quick';

const Home = async () => {
  return (
    <>
      <h1>Home</h1>
      {/* <Quick></Quick> */}
      {/* HOME {user ? `Welcome, ${user}` : 'Not logged in'} */}
    </>
  );
};

export default Home;
